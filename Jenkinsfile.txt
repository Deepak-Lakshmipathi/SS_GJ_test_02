pipeline {
    agent any

    stages {

        stage('Compile') {
            steps {
                echo "Compiling..."
                sudo sh "/home/ec2-user/sbt/bin/sbt compile"
            }
        }

        stage('Package') {
            steps {
                echo "Packaging..."
                sudo sh "/home/ec2-user/sbt/bin/sbt package"
            }
        }

    }
}