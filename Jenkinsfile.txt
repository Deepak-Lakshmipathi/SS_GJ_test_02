pipeline {
    agent any

    stages {

        stage('Package') {
            steps {
                echo "Packaging..."
                sh "/home/ec2-user/sbt/bin/sbt -mem 512 package"
            }
        }

    }
}