pipeline {
    agent any

    stages {

        stage('Package') {
            steps {
                echo "Packaging..."
                sh "/usr/share/sbt/bin/sbt -mem 1024 package"
            }
        }

    }
}