pipeline {
    agent any

    stages {

        stage('Compile') {
            steps {
                echo "Compiling..."
                sh "/home/ec2-user/sbt/bin/sbt compile"
            }
        }

        stage('Package') {
            steps {
                echo "Packaging..."
                sh "/home/ec2-user/sbt/bin/sbt package"
            }
        }

    }
}